<Page x:ConnectionId='1'
    x:Class="NorthwindDemo.OrderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NorthwindDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:ConnectionId='2' Background="{ThemeResource SystemControlAcrylicElementBrush}" 
          AllowDrop="True"  
                                       
                               >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:ConnectionId='3' Name="HiddenContainer" Grid.RowSpan="2" Opacity="0" >
            <Grid x:ConnectionId='14' Name="DirectPrintContainer">
                <StackPanel x:ConnectionId='15' Name="PrintableContent"
                            Orientation="Vertical">
                    <TextBlock x:ConnectionId='16' Name="PrintText" 
                               FontSize="10" FontFamily="Courier New" Margin="10"
                               Text="This is the test that will be sent to the printer."
                               TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Grid>

        <Grid x:ConnectionId='4' Name="MainGrid" Grid.Row="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <CommandBar Grid.Row="0" Grid.Column="1" Background="{ThemeResource SystemControlAcrylicElementBrush}" 
                        ClosedDisplayMode="Compact" IsSticky="True" >
                <AppBarButton x:ConnectionId='9' Name="Pay" Icon="Shop" Label="Pay" AccessKey="P"
                                               />
                <AppBarButton x:ConnectionId='10' Name="Save" Icon="Save" Label="Save" AccessKey="S"
                                                />
                <AppBarButton x:ConnectionId='11' Name="Reprint" Icon="Print" Label="Reprint" AccessKey="R" Visibility="Collapsed"
                                                   />
                <AppBarSeparator/>
                <AppBarToggleButton x:ConnectionId='12' Name="Toggle" Icon="Switch" Label="Toggle" AccessKey="T"
                                                        />
                <CommandBar.Content>
                    <ComboBox x:ConnectionId='13' Name="CustomerComboBox"
                              PlaceholderText="Select customer" PlaceholderForeground="DarkRed"
                              Width="200" HorizontalAlignment="Center" Margin="12" VerticalAlignment="Center"
                              DataContext="Customers"                                  DisplayMemberPath="CompanyName"
                              />
                </CommandBar.Content>
            </CommandBar>
            
            <TextBlock x:ConnectionId='5' Text="Submit New Order" Grid.Row="1" Grid.Column="1"
                       Name="OrderPageHeading" FontSize="18" Margin="12"
                       HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" />

            <TextBlock x:ConnectionId='6' Text="Drop Northwind products here" Grid.Row="2" Grid.Column="1"
                       Name="Instructions" FontStyle="Italic"
                       HorizontalAlignment="Center" Foreground="SlateGray" />
            
            <ListView x:ConnectionId='7' Name="OrderDetailsListView" Grid.Row="3" Grid.Column="1"
                      DataContext="OrderDetails"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.IsVerticalRailEnabled="True"
                      ScrollViewer.VerticalScrollMode="Enabled"
                      ScrollViewer.HorizontalScrollMode="Enabled"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.IsHorizontalRailEnabled="True" >
                <ListView.HeaderTemplate>
                    <DataTemplate >
                        <StackPanel Orientation="Horizontal"  >
                            <TextBlock Text="Qty" Margin="12, 0"  Width="50" Foreground="DarkRed" />
                            <TextBlock Text="Description"  Width="220" Foreground="DarkRed" />
                            <TextBlock Text="Per unit" Width="80" Foreground="DarkRed" />
                            <TextBlock Text="Net price"  Width="80" Foreground="DarkRed" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <StackPanel Orientation="Horizontal" >
                            <TextBox x:ConnectionId='8' Name="QuantityTextbox" Text="{Binding Quantity, Mode=TwoWay}"
                                                                              
                                     Width="50" />
                            <TextBox Text="{Binding ProductName}"
                                     IsReadOnly="True"
                                     Width="220" />
                            <TextBox Name="UnitPriceTextBox" Text="{Binding UnitPriceString, Mode=TwoWay}"
                                     IsReadOnly="true"
                                     Width="80" />
                            <TextBox Name="NetAmountTextBox"
                                     IsReadOnly="True"
                                     Width="80" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
             </ListView>
        </Grid>
        
    </Grid>
</Page>

